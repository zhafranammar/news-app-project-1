<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>News App Management</title>
    <!-- Tailwind CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet" />
    <style>
      body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }

      footer {
        margin-top: auto;
      }
    </style>
  </head>

  <body class="bg-gray-100">
    <!-- Header -->
    <header class="bg-blue-500 text-white p-4">
      <div class="container mx-auto">
        <a href="/"
          ><h1 class="text-2xl font-bold">News Management System</h1></a
        >
      </div>
    </header>

    <div class="container mx-auto mt-8">
      <h2 class="text-2xl font-bold mb-4">User Details</h2>
      <div
        class="max-w-md mx-auto bg-white rounded-xl shadow-md overflow-hidden md:max-w-2xl">
        <div class="p-8">
          <div
            class="uppercase tracking-wide text-sm text-indigo-500 font-semibold">
            Username
          </div>
          <p class="mt-2 text-gray-900"><%= user.username %></p>
          <div
            class="uppercase tracking-wide text-sm text-indigo-500 font-semibold mt-4">
            Email
          </div>
          <p class="mt-2 text-gray-900"><%= user.email %></p>
          <div
            class="uppercase tracking-wide text-sm text-indigo-500 font-semibold mt-4">
            Phone
          </div>
          <p class="mt-2 text-gray-900"><%= user.phone %></p>

          <div class="flex justify-end m-8">
            <a
              href="/users/<%= user.id %>/edit"
              class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
              >Edit</a
            >
          </div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white p-4">
      <div class="container mx-auto text-center">
        <p>&copy; 2024 News Management System</p>
      </div>
    </footer>
  </body>
</html>
